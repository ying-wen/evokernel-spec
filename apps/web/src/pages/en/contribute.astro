---
// EN mirror of /contribute. Content is locale-conditional in the
// shared component, so we just import and pass locale="en".
import ContributePage from '../contribute.astro';
---
<ContributePage locale="en" />
