---
/**
 * /agents/ — integration page for agent builders.
 *
 * v3.31 status: corpus JSON API + MCP + productized CLI + host-LLM +
 * synthesized unknown-model bundles + 4-entry technique catalog.
 */
import BaseLayout from '~/layouts/BaseLayout.astro';
import Container from '~/components/ui/Container.astro';
import SectionHeader from '~/components/ui/SectionHeader.astro';
import { pathname } from '~/lib/i18n';
---
<BaseLayout title="Agents · 智能体集成">
  <Container>
    <section class="py-12">
      <SectionHeader
        eyebrow="AGENTS · v3.31"
        title="给智能体的集成入口"
        subtitle="让任意模型 × 任意硬件部署智能体读取 corpus、agent-context、technique catalog，并进入 productized deploy loop"
      />
      <p class="text-sm mt-4 max-w-3xl" style="color: var(--color-text-muted);">
        EvoKernel Spec 不只是给人读的网站。v3.31 以后，corpus 同时通过 24 个 JSON API endpoint、MCP tools、Codex/Claude Code plugin、
        <code>/api/agent-context/*</code> bundle 和 <code>/techniques/</code> 暴露，让外部智能体可以把它当作
        <strong>"任意模型 × 任意硬件 编译/优化"</strong> 的结构化知识库和部署执行入口。
      </p>
    </section>

    {/* The 7-stage agent pipeline */}
    <section class="pb-8 border-t pt-8" style="border-color: var(--color-border);">
      <h2 class="text-2xl font-semibold mb-3">📐 智能体 pipeline 与本站的对应</h2>
      <p class="text-sm mb-4 max-w-3xl" style="color: var(--color-text-muted);">
        一个端到端 "model × hw → deploy" 智能体通常有 7 个阶段. 本站为每个阶段提供数据 / 工具:
      </p>
      <div class="overflow-x-auto">
        <table class="text-sm w-full" style="border-collapse: collapse;">
          <thead>
            <tr style="border-bottom: 2px solid var(--color-border);">
              <th class="text-left p-3" style="min-width: 60px;">#</th>
              <th class="text-left p-3" style="min-width: 200px;">阶段</th>
              <th class="text-left p-3">本站提供</th>
              <th class="text-left p-3" style="min-width: 100px;">完整度</th>
            </tr>
          </thead>
          <tbody>
            <tr style="border-bottom: 1px solid color-mix(in oklch, var(--color-border) 50%, transparent);">
              <td class="p-3 font-mono">1</td>
              <td class="p-3 font-semibold">Model understanding</td>
              <td class="p-3 text-xs" style="color: var(--color-text-muted);">
                <code>/api/models.json</code> — 34 frontier / scientific / multimodal 模型 · arch / family / params /
                operator decomposition
              </td>
              <td class="p-3"><span style="color: oklch(70% 0.18 70);">●</span> 70%</td>
            </tr>
            <tr style="border-bottom: 1px solid color-mix(in oklch, var(--color-border) 50%, transparent);">
              <td class="p-3 font-mono">2</td>
              <td class="p-3 font-semibold">Hardware understanding</td>
              <td class="p-3 text-xs" style="color: var(--color-text-muted);">
                <code>/api/hardware.json</code> — 64 张卡 · compute / memory / power-thermal / scale-up / scale-out
              </td>
              <td class="p-3"><span style="color: oklch(60% 0.18 145);">●</span> 80%</td>
            </tr>
            <tr style="border-bottom: 1px solid color-mix(in oklch, var(--color-border) 50%, transparent);">
              <td class="p-3 font-mono">3</td>
              <td class="p-3 font-semibold">Cross-vendor op equivalence</td>
              <td class="p-3 text-xs" style="color: var(--color-text-muted);">
                <code>/api/operators.json</code> + <code>/api/fused-kernels.json</code> + <code>/api/techniques.json</code> · per-arch fitness via <a href={pathname('/operators/hardware-fitness/')} class="underline" style="color: var(--color-accent);">/operators/hardware-fitness/</a>
              </td>
              <td class="p-3"><span style="color: oklch(70% 0.18 70);">●</span> 55%</td>
            </tr>
            <tr style="border-bottom: 1px solid color-mix(in oklch, var(--color-border) 50%, transparent);">
              <td class="p-3 font-mono">4</td>
              <td class="p-3 font-semibold">Constraint solve / planning</td>
              <td class="p-3 text-xs" style="color: var(--color-text-muted);">
                <code>/api/solve.json</code> + <code>/api/agent-context-index.json</code> + <code>/api/agent-context/&lt;model&gt;-on-&lt;hardware&gt;.json</code> — planning prior + bundle context
              </td>
              <td class="p-3"><span style="color: oklch(60% 0.18 145);">●</span> 75%</td>
            </tr>
            <tr style="border-bottom: 1px solid color-mix(in oklch, var(--color-border) 50%, transparent);">
              <td class="p-3 font-mono">5</td>
              <td class="p-3 font-semibold">Codegen (config + kernel)</td>
              <td class="p-3 text-xs" style="color: var(--color-text-muted);">
                Productized CLI generates kernels through host-LLM / real / cache / test / skeleton modes; unknown HF models can synthesize temporary bundles.
              </td>
              <td class="p-3"><span style="color: oklch(70% 0.18 70);">●</span> 65%</td>
            </tr>
            <tr style="border-bottom: 1px solid color-mix(in oklch, var(--color-border) 50%, transparent);">
              <td class="p-3 font-mono">6</td>
              <td class="p-3 font-semibold">Validation</td>
              <td class="p-3 text-xs" style="color: var(--color-text-muted);">
                V1 structural build · V2 correctness/reference compare · tensor-diff utility · V3 profiler parsers. Cross-arch execution still v3.32+.
              </td>
              <td class="p-3"><span style="color: oklch(70% 0.18 70);">●</span> 60%</td>
            </tr>
            <tr>
              <td class="p-3 font-mono">7</td>
              <td class="p-3 font-semibold">Deploy + observe</td>
              <td class="p-3 text-xs" style="color: var(--color-text-muted);">
                `--remote` / `--execute` SSH build-run-profile path · emitted Dockerfile/K8s/runbook/SBOM · agent-learning feedback loop.
              </td>
              <td class="p-3"><span style="color: oklch(70% 0.18 70);">●</span> 70%</td>
            </tr>
          </tbody>
        </table>
      </div>
      <p class="text-xs mt-3" style="color: var(--color-text-muted);">
        总体 agent-readiness 已经从 v2.x 的查询型 API 进入 v3.x 的 productized loop。当前最大缺口不是入口能力，而是数据覆盖、cross-arch verify execution、serving/client-test 和 synthesized bundle 持久化。路线图见
        <a href={pathname('/changelog/')} class="underline" style="color: var(--color-accent);">/changelog/</a>
        和 docs/superpowers/specs/2026-05-02-agent-readiness.md (GitHub).
      </p>
    </section>

    {/* JSON API endpoints */}
    <section class="py-8 border-t" style="border-color: var(--color-border);">
      <h2 class="text-2xl font-semibold mb-3">📡 JSON API endpoints</h2>
      <p class="text-sm mb-4 max-w-3xl" style="color: var(--color-text-muted);">
        全部静态生成, CDN 缓存. 数据 CC-BY-SA 4.0.
      </p>
      <div class="grid gap-3">
        {[
          { path: '/api/index.json', desc: '入口 · 计数 · 链接到所有 endpoint' },
          { path: '/api/hardware.json', desc: '64 张卡 · compute / memory / power-thermal' },
          { path: '/api/models.json', desc: '34 模型 · arch / op decomposition' },
          { path: '/api/cases.json', desc: '41 个实测 case · stack + scenario + results' },
          { path: '/api/operators.json', desc: '38 算子 · FLOPs / bytes / arith intensity / engine impls' },
          { path: '/api/fused-kernels.json', desc: '27 fused kernels · operators_folded + engine coverage' },
          { path: '/api/techniques.json', desc: 'v3.30 · research technique / porting catalog · Sage/Flash/Paged/Ring Attention' },
          { path: '/api/playbooks.json', desc: '24 (model × hw class) recipes' },
          { path: '/api/agent-context-index.json', desc: '2176+ model × hardware bundles · static context index' },
          { path: '/api/solve.json', desc: '所有配置的 flat enumeration + derived $/M tokens · 客户端 filter' },
          { path: '/api/openapi.json', desc: 'OpenAPI 3.1 spec · v3.31 endpoint parity baseline' },
          { path: '/api/health.json', desc: '健康检查 (body, HTTP 200 due to SSG)' }
        ].map(({ path, desc }) => (
          <a href={pathname(path)} class="rounded-lg p-3 border flex items-center gap-3 transition-transform hover:translate-x-0.5"
             style="background: var(--color-surface-raised); border-color: var(--color-border);">
            <code class="font-mono text-sm" style="color: var(--color-accent); font-weight: 500; min-width: 240px;">{path}</code>
            <span class="text-sm" style="color: var(--color-text-muted);">{desc}</span>
          </a>
        ))}
      </div>
    </section>

    {/* Solve example */}
    <section class="py-8 border-t" style="border-color: var(--color-border);">
      <h2 class="text-2xl font-semibold mb-3">🎯 /api/solve.json — 约束求解器</h2>
      <p class="text-sm mb-4 max-w-3xl" style="color: var(--color-text-muted);">
        SSG 静态站不能接受 query params, 所以 /api/solve.json 返回所有已知配置的 flat array, 客户端 filter.
        每个 entry 已经做好规范化 (统一 shape) + derived field (估算 $/M tokens, default_score).
      </p>
      <div class="rounded-lg p-4 border" style="background: var(--color-surface-raised); border-color: var(--color-border);">
        <pre class="text-xs overflow-x-auto" style="white-space: pre-wrap; font-family: var(--font-mono); color: var(--color-text);"><code>{`// 示例 1: 找 H100 上 $/M tokens < 1.0 的所有配置, 按价格排序
const r = await fetch('https://yingwen.io/evokernel-spec/api/solve.json').then(r => r.json());
r.configurations
  .filter(c => c.hardware.id === 'h100-sxm5'
            && (c.metrics.dollars_per_m_tokens_estimate ?? 99) < 1.0)
  .sort((a, b) => (a.metrics.dollars_per_m_tokens_estimate ?? 99)
                - (b.metrics.dollars_per_m_tokens_estimate ?? 99));

// 示例 2: 国产硬件选项 (合规需求)
r.configurations.filter(c => ['huawei','cambricon','moore-threads','hygon']
  .includes(c.hardware.vendor ?? ''));

// 示例 3: 每张卡 decode 吞吐 > 10K tok/s 的所有 measured case
r.configurations
  .filter(c => c.tier === 'measured'
            && (c.metrics.decode_throughput_tok_s_per_card ?? 0) > 10000);

// 示例 4: 严格 TTFT p99 < 300ms (低延迟应用)
r.configurations.filter(c => (c.metrics.ttft_p99_ms ?? 99999) < 300);
`}</code></pre>
      </div>
      <p class="text-xs mt-3" style="color: var(--color-text-muted);">
        响应包括 <code>query_examples</code> 字段, 列出常用 filter 模式. <code>default_score</code> 字段提供综合排名 (0.5 throughput + 0.3 cost + 0.2 latency 加权).
      </p>
    </section>

    {/* MCP roadmap */}
    <section class="py-8 border-t" style="border-color: var(--color-border);">
      <h2 class="text-2xl font-semibold mb-3">🗺️ MCP / Plugin surfaces</h2>
      <p class="text-sm mb-4 max-w-3xl" style="color: var(--color-text-muted);">
        MCP server、Codex binary、Claude Code slash command、Cursor rules 都已经存在。JSON API 是静态可缓存的数据面，MCP/plugin 是智能体的交互面。
      </p>
      <div class="grid gap-3 sm:grid-cols-2">
        {[
          { tool: 'query_hardware(filter)', desc: '基于约束查找硬件 — eg "HBM > 96GB && cooling = air"' },
          { tool: 'query_operator_fitness(op_id, hw_arch)', desc: '查找 (op, arch) 的 engine implementations + 等价 kernel pointer' },
          { tool: 'solve_config(query)', desc: '同 /api/solve.json 但接受 structured query, 服务端 filter+排名' },
          { tool: 'get_case(id)', desc: '获取完整 reproducible case (含 startup_command + config_files)' },
          { tool: 'agent_resolve_bundle(model, hardware)', desc: '解析/列出 agent-context bundle，供 planning 和 codegen 使用' },
          { tool: 'agent_auto_pr()', desc: '聚合 agent-learning，生成 PR-ready corpus update draft' },
          { tool: 'get_technique(id)', desc: '查询 SageAttention 等 technique 的适用模型/算子/硬件和 port_targets' },
          { tool: 'predict_fit(model, hw, batch, ctx_len)', desc: '在不实际运行的前提下, 预测 OOM / throughput / TTFT 范围' }
        ].map(({ tool, desc }) => (
          <div class="rounded-lg p-4 border" style="background: var(--color-surface-raised); border-color: var(--color-border);">
            <code class="text-xs font-mono block mb-1" style="color: var(--color-accent); font-weight: 600;">{tool}</code>
            <p class="text-xs" style="color: var(--color-text-muted);">{desc}</p>
          </div>
        ))}
      </div>
    </section>

    {/* Known gaps */}
    <section class="py-8 border-t" style="border-color: var(--color-border);">
      <h2 class="text-2xl font-semibold mb-3" style="color: oklch(70% 0.18 70);">⚠️ 已知 gap (v3.32+)</h2>
      <ol class="space-y-3 max-w-3xl">
        <li class="rounded p-4 border" style="background: var(--color-surface-raised); border-color: var(--color-border);">
          <div class="text-sm font-semibold mb-1">API descriptor / OpenAPI 需要测试锁住 parity</div>
          <p class="text-xs" style="color: var(--color-text-muted);">
            v3.31 已把 `/api/index.json`、`/api/health.json`、`/api/openapi.json` 对齐到当前主要 route；下一步要加自动测试，防止后续 endpoint 漂移。
          </p>
        </li>
        <li class="rounded p-4 border" style="background: var(--color-surface-raised); border-color: var(--color-border);">
          <div class="text-sm font-semibold mb-1">数据覆盖仍不足以支撑真正“任意”</div>
          <p class="text-xs" style="color: var(--color-text-muted);">
            schema 通过不等于 agent-ready。当前 audit 仍有硬件数值 WARN、无 case 硬件/模型、model graph 缺口、reference impl 缺口和 engine workflow 缺口。
          </p>
        </li>
        <li class="rounded p-4 border" style="background: var(--color-surface-raised); border-color: var(--color-border);">
          <div class="text-sm font-semibold mb-1">Cross-arch numerical verify execution 未闭环</div>
          <p class="text-xs" style="color: var(--color-text-muted);">
            tensor-diff utility 已有，但还要把 reference arch、target arch、technique tolerance、remote-target 输出连接成一条可执行链。
          </p>
        </li>
        <li class="rounded p-4 border" style="background: var(--color-surface-raised); border-color: var(--color-border);">
          <div class="text-sm font-semibold mb-1">Synthesized bundle 还没有持久化回 corpus</div>
          <p class="text-xs" style="color: var(--color-text-muted);">
            v3.29+ 能让未知 HF 模型进入 productized loop，但成功后仍应输出 PR-ready `data/models/` / `data/model-graphs/` stub，避免下一次重新 synthesize。
          </p>
        </li>
        <li class="rounded p-4 border" style="background: var(--color-surface-raised); border-color: var(--color-border);">
          <div class="text-sm font-semibold mb-1">网页导航和英文镜像仍需 route parity gate</div>
          <p class="text-xs" style="color: var(--color-text-muted);">
            中文 canonical 页面覆盖更全；英文镜像是 partial。导航、`hreflang` 和 generated links 需要 build 后检测，不能继续产生隐藏 404。
          </p>
        </li>
      </ol>
    </section>

    {/* CTA */}
    <section class="pb-12 pt-6 border-t" style="border-color: var(--color-border);">
      <div class="rounded-lg p-6 border" style="background: color-mix(in oklch, var(--color-accent) 5%, var(--color-surface-raised)); border-color: var(--color-accent);">
        <h3 class="text-lg font-semibold mb-2">让我们知道你怎么用</h3>
        <p class="text-sm mb-3" style="color: var(--color-text-muted);">
          如果你正在用本站的数据构建智能体 / 工具, 欢迎告诉我们 — 你的真实使用场景能帮我们优先级排序 v2.5 / v2.6 / v2.7 的 gap 闭环.
        </p>
        <ul class="text-sm space-y-1.5" style="color: var(--color-text);">
          <li>· GitHub Discussions: <a href="https://github.com/evokernel/evokernel-spec/discussions" target="_blank" rel="noopener" class="underline" style="color: var(--color-accent);">/evokernel-spec/discussions</a></li>
          <li>· Issue with <code>agent-integration</code> label: <a href="https://github.com/evokernel/evokernel-spec/issues" target="_blank" rel="noopener" class="underline" style="color: var(--color-accent);">/evokernel-spec/issues</a></li>
          <li>· 设计文档: <code>docs/superpowers/specs/2026-05-02-agent-readiness.md</code> (GitHub)</li>
        </ul>
      </div>
    </section>
  </Container>
</BaseLayout>
