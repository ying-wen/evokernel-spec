node_modules/
dist/
.astro/
.cloudflare/
*.log
.env
.env.local
.DS_Store
coverage/
playwright-report/
test-results/
.pnpm-store/
.runtime/
agent-deploy-output/

# v3.4: LLM-generated kernel cache (committable subset goes elsewhere)
.cache/

scripts/tests/fixtures/dist-bundle-fixture/
scripts/tests/fixtures/v3-26-targets/

# v3.26 -- per-pair watch output dir
agent-watch-output/

# v3.26 -- local SSH targets config. The .example file is committed;
# the real one (with private host info) lives in ~/.config/evokernel/
# per docs/SECURITY-NOTES.md and must NEVER be committed to public repo.
# (Defensive — repo doesn't reference this path, but git-ignore protects
# any contributor who accidentally drops a real config in the repo tree.)
**/targets.yaml
!**/targets.yaml.example

# v3.28 -- per-deploy output directories created by `agent:deploy --output`.
# These contain run logs that may include redacted-but-not-redacted-enough
# remote-host references plus per-deploy artifacts that should not be
# checked in. The default RUNBOOK examples use `./out/<name>/`.
out/

# Local Claude/Codex worktrees are user/session state, not project source.
.claude/worktrees/
