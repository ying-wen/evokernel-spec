id: mooncake-kv-disaggregation
name: Mooncake KV Disaggregation
category: kv-cache-management
summary: |
  把 prefill 与 decode 物理拆到不同节点群上跑, prefill 节点把 KV cache 通过高速 RDMA
  传到 decode 节点, 让 decode 节点不被长 prompt 阻塞. Moonshot/Kimi 在 production 上
  把 H100 利用率从 30% 提到 80%+.

description_md: |
  ## 问题: 长 prompt 阻塞 decode

  传统 vLLM continuous batching 把 prefill 与 decode 在同一卡上调度. 一个 32K context 的
  prompt 进来, prefill 阶段会**独占 GPU 1-3 秒**, 期间所有正在 decode 的请求 TBT 飙升.

  这个问题在 RAG / 长文档 / agent 场景下尤其严重: 平均请求 prompt 长度从 ~500 token
  涨到 8000+, prefill 时间从 50 ms 涨到 1500 ms.

  ## Mooncake 的方案

  - **Prefill 节点群**: 专门跑 prefill, 满 SM 利用. KV cache 算完后**不丢**,
    通过 RDMA 推送到 decode 节点
  - **Decode 节点群**: 专门跑 decode, 接收预算好的 KV. SM 利用率低 (memory-bound) 但
    这是 decode 本身的特性, 不是浪费
  - **KV transfer**: 用 RDMA over Converged Ethernet (RoCE) 或 InfiniBand 直接 GPU
    显存到显存, 单次 transfer ~30 ms (32K context, ~5 GB KV)
  - **Cache pool**: 多层 KV cache (HBM hot, host RAM warm, NVMe cold) — Mooncake
    的"分级 KV 池"

  ## Fused 是什么意思

  这里"fused" 不是指传统的 kernel 内 op 融合, 而是指 **scheduling-level fusion**:
  把 KV-cache write (prefill 阶段) + KV-cache transfer (跨节点 RDMA) + KV-cache read
  (decode 阶段) 当作一个**逻辑流水**统一调度. vLLM 0.6+ 的 disaggregated_prefill 实现
  正是这个思路.

  ## 引擎落地

  - **vLLM**: PR #10502 起官方支持 prefill/decode disaggregation; KV transfer 走
    NIXL (NVIDIA Internode Communication Library) 或 LMCache
  - **SGLang**: PD-disaggregation native 支持, RadixAttention 与之协同
  - **TRT-LLM**: NVIDIA Dynamo 框架的核心特性
  - **Moonshot/Kimi**: production 部署框架开源在 [Mooncake repo](https://github.com/kvcache-ai/Mooncake)

fuses_operators: [attention, kv-write, allreduce]
why_fuse: |
  Prefill 与 decode 的硬件需求完全不同 (compute-bound vs memory-bound), 强行同卡跑等于
  让 70% 的 SM 浪费. Disaggregation 让两类工作匹配最适合的硬件: prefill 用高 FLOPs 卡,
  decode 用大显存卡. KV transfer 是关键工程: 通过 RDMA + 异步 streaming 把开销控制在
  1-3% 总延迟.

speedup:
  - baseline: 'Same-node continuous batching (vLLM default)'
    multiplier_min: 2.5
    multiplier_max: 4.0
    notes: 'aggregate throughput 收益, 高 prompt 长度场景更显著'
  - baseline: 'P99 TBT (time between tokens)'
    multiplier_min: 3
    multiplier_max: 8
    notes: '长 prompt 不再阻塞 decode, P99 latency 显著下降'

implementations:
  - name: Mooncake (Moonshot)
    hardware_arch: [hopper, ampere, blackwell]
    url: https://github.com/kvcache-ai/Mooncake
    notes: 'reference implementation; production-tested at Kimi 千万 DAU 级'
  - name: vLLM disaggregated_prefill
    engine_id: vllm
    hardware_arch: [hopper, ampere, blackwell]
    url: https://docs.vllm.ai/en/latest/serving/disagg_prefill.html
    notes: 'NIXL or LMCache transport; 0.6.4+'
  - name: SGLang PD-disaggregation
    engine_id: sglang
    hardware_arch: [hopper, ampere, blackwell]
    url: https://docs.sglang.ai/router/pd_disaggregation.html
  - name: NVIDIA Dynamo framework
    engine_id: tensorrt-llm
    hardware_arch: [hopper, ada, blackwell]
    url: https://github.com/ai-dynamo/dynamo

hardware_requires:
  - 'High-bandwidth scale-out fabric: ≥ 400 Gbps RDMA between nodes (IB NDR / RoCE 400G)'
  - 'GPUDirect RDMA capability (skip CPU host memory in KV transfer)'
  - '至少 2 个 node — 单 node 部署 disagg 没有意义'

enables_patterns: [disaggregated-prefill-decode]

applies_at_stage: serve

trade_offs:
  - '复杂度上升: 多了 router + KV cache pool 调度组件; SLO 要重新建模'
  - 'KV transfer 延迟: 即使 1-2 ms RDMA 也会增加 TTFT (但 P99 TBT 显著改善)'
  - '硬件错配风险: prefill node 选 H100 (FLOP-rich), decode node 选 H200 (BW-rich) 才发挥最佳; 同质化部署可能反慢'
  - '故障域增加: prefill 节点宕机会让 in-flight 的 KV transfer 丢失, 需要 retry 逻辑'

references:
  - title: 'Mooncake: A KVCache-centric Disaggregated Architecture for LLM Serving'
    url: https://arxiv.org/abs/2407.00079
    source_type: paper
  - title: 'Mooncake repository (Moonshot)'
    url: https://github.com/kvcache-ai/Mooncake
    source_type: implementation
  - title: 'NVIDIA Dynamo'
    url: https://github.com/ai-dynamo/dynamo
    source_type: implementation
  - title: 'vLLM disaggregated prefill design doc'
    url: https://docs.vllm.ai/en/latest/serving/disagg_prefill.html
    source_type: docs

formal_semantics:
  signature: |
    mooncake_kv_disagg(prefill_kv: [num_layers, B, H_kv, S, D_head] (on prefill GPU),
                       transfer_handle, decode_target_gpu)
        -> kv_loaded_on_decode_gpu: bool
    Streams KV cache from a prefill-specialized GPU pool to a decode-specialized
    GPU pool over GPUDirect RDMA / NVLink Fabric. Allows separate scaling of
    prefill capacity (compute-heavy) vs decode capacity (memory-heavy).

  fusion_lifecycle: manual-kernel

  unfused_penalty: |
    Without disaggregation: prefill and decode share GPU resources. At long-prefill
    workloads (RAG, code), prefill bursts starve decode latency for ~hundreds of
    ms. With Mooncake: dedicated prefill pool processes the burst; KV transferred
    via RDMA in <50ms; decode pool stays unaffected.

  edge_cases:
    - input: 'KV transfer over IB / RoCE vs NVLink Fabric'
      behaviors:
        mooncake-rdma: 'Uses GPUDirect RDMA over IB-NDR (400Gbps) or RoCEv2 — typical 50ms for 70B-model 32K-ctx KV.'
        mooncake-nvlink: 'Intra-rack via NVLink Fabric — 10x faster (5ms).'
        nixl-internode: 'NVIDIA NIXL backend; equivalent path with KvBlocks abstraction.'
      mitigation: 'Co-locate prefill and decode pools in same NVLink island when possible (NVL72). Cross-rack disagg only if absolutely needed for capacity.'

    - input: 'KV layout compatibility (paged-attention page size, dtype)'
      behaviors:
        all_libs: 'Prefill and decode pools MUST use same page_size, dtype, and num_layers/heads/dim. Otherwise transfer is invalid.'
      mitigation: 'Single engine version + identical config across pools. Mismatch = silent corruption.'

    - input: 'multi-tenancy (multiple users sharing pool)'
      behaviors:
        mooncake-multi-tenant: 'Per-request KV transfer; each request has its own RDMA target.'
      mitigation: 'Mature: KV ownership tracked via request_id. Concurrent transfers coexist via RDMA queue pairs.'

  numerical_rules:
    - aspect: 'no_arithmetic_no_precision_concern'
      per_library:
        all_libs: 'Pure data movement; bit-identical on both sides.'
      notes: 'Same as memcpy-async. The only correctness concern is layout/dtype/version match.'

    - aspect: 'transfer_failure_handling'
      per_library:
        mooncake: 'On transfer fail: decode pool falls back to running prefill itself (degraded latency but correct output).'
        nixl: 'Same fallback contract.'
      notes: 'Critical for production: transfer must be best-effort with fallback, not blocking. SLO-aware design.'

  reference_impl:
    framework: pytorch
    snippet: |
      # Pseudo: the actual transfer is via Mooncake/NIXL C++ runtime, not Python
      class MooncakeKvTransfer:
          def push_kv(self, kv_tensor, dst_gpu_id, request_id):
              # Async RDMA put; returns transfer handle
              return rdma_put(kv_tensor, dst_gpu_id, tag=request_id)
          def wait(self, handle, timeout_ms=200):
              # Decode pool blocks here until KV arrives or timeout
              return rdma_wait(handle, timeout_ms)
